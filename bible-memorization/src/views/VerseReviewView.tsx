import { Text, View } from 'react-native';

export const VerseReviewView = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text> Verse Review View</Text>
        </View>
    );
}