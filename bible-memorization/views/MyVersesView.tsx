import { Text, View } from 'react-native';

export const MyVersesView = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text> My Verse view</Text>
        </View>
    );
}